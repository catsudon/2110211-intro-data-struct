    // if ((*it).first >= pos and !mark)
        // {
        //     v.insert(it, {pos, value});
        //     mark = 1;
        // }
        // if ((*it).first >= pos and mark)
        //     v.insert(it, {(*it).first + 1, (*it).second});